server:
  port: ${PORT:8080}
  shutdown: graceful
  forward-headers-strategy: none
  max-http-request-header-size: 16KB
  tomcat:
    connection-timeout: 5s
    keep-alive-timeout: 15s
    max-keep-alive-requests: 100
    max-http-form-post-size: 16KB
    max-swallow-size: 16KB
    max-parameter-count: 32

spring:
  application:
    name: catalog-service
  jackson:
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: true
  lifecycle:
    timeout-per-shutdown-phase: 30s
  threads:
    virtual:
      enabled: true
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:55434/catalog}
    username: ${DATABASE_USERNAME:catalog}
    password: ${DATABASE_PASSWORD:catalog-local-only}
    hikari:
      pool-name: catalog-pool
      maximum-pool-size: ${DATABASE_POOL_MAX_SIZE:10}
      minimum-idle: ${DATABASE_POOL_MIN_IDLE:1}
      connection-timeout: 3000
      validation-timeout: 1000
      max-lifetime: 1500000
  flyway:
    enabled: ${SPRING_FLYWAY_ENABLED:false}
    clean-disabled: true
    validate-on-migrate: true
  jooq:
    sql-dialect: postgres

app:
  circulation-consumer:
    enabled: ${CIRCULATION_CONSUMER_ENABLED:false}
    instance-id: ${CIRCULATION_CONSUMER_INSTANCE_ID:${HOSTNAME:local}}
    group-id: ${CIRCULATION_CONSUMER_GROUP_ID:catalog-availability-v1}
    topic: ${CIRCULATION_CONSUMER_TOPIC:mundia.circulation.events.v1}
    schema-subject: ${CIRCULATION_CONSUMER_SCHEMA_SUBJECT:mundia.circulation.v1.CirculationEvent}
    schema-version: ${CIRCULATION_CONSUMER_SCHEMA_VERSION:1}
    poll-timeout: ${CIRCULATION_CONSUMER_POLL_TIMEOUT:PT0.5S}
    commit-timeout: ${CIRCULATION_CONSUMER_COMMIT_TIMEOUT:PT5S}
    retry-backoff: ${CIRCULATION_CONSUMER_RETRY_BACKOFF:PT0.25S}
    startup-grace-period: ${CIRCULATION_CONSUMER_STARTUP_GRACE:PT30S}
    maximum-poll-silence: ${CIRCULATION_CONSUMER_MAXIMUM_POLL_SILENCE:PT30S}
    maximum-poll-records: ${CIRCULATION_CONSUMER_MAXIMUM_POLL_RECORDS:50}
    maximum-event-bytes: ${CIRCULATION_CONSUMER_MAXIMUM_EVENT_BYTES:262144}
    kafka:
      bootstrap-servers: ${CIRCULATION_CONSUMER_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
      security-protocol: ${CIRCULATION_CONSUMER_KAFKA_SECURITY_PROTOCOL:SASL_SSL}
      allow-insecure-transport: ${CIRCULATION_CONSUMER_KAFKA_ALLOW_INSECURE_TRANSPORT:false}
      sasl-mechanism: ${CIRCULATION_CONSUMER_KAFKA_SASL_MECHANISM:SCRAM-SHA-512}
      sasl-jaas-config: ${CIRCULATION_CONSUMER_KAFKA_SASL_JAAS_CONFIG:}
      truststore-location: ${CIRCULATION_CONSUMER_KAFKA_TRUSTSTORE_LOCATION:}
      truststore-password: ${CIRCULATION_CONSUMER_KAFKA_TRUSTSTORE_PASSWORD:}
      keystore-location: ${CIRCULATION_CONSUMER_KAFKA_KEYSTORE_LOCATION:}
      keystore-password: ${CIRCULATION_CONSUMER_KAFKA_KEYSTORE_PASSWORD:}
      key-password: ${CIRCULATION_CONSUMER_KAFKA_KEY_PASSWORD:}
      request-timeout: ${CIRCULATION_CONSUMER_KAFKA_REQUEST_TIMEOUT:PT10S}
      session-timeout: ${CIRCULATION_CONSUMER_KAFKA_SESSION_TIMEOUT:PT30S}
      heartbeat-interval: ${CIRCULATION_CONSUMER_KAFKA_HEARTBEAT_INTERVAL:PT3S}
  outbox:
    enabled: ${OUTBOX_DELIVERY_ENABLED:false}
    instance-id: ${OUTBOX_INSTANCE_ID:${HOSTNAME:local}}
    topic: ${OUTBOX_TOPIC:mundia.catalog.events.v1}
    schema-subject: ${OUTBOX_SCHEMA_SUBJECT:mundia.catalog.v1.CatalogEvent}
    poll-interval: ${OUTBOX_POLL_INTERVAL:PT0.5S}
    lease-duration: ${OUTBOX_LEASE_DURATION:PT1M30S}
    batch-size: ${OUTBOX_BATCH_SIZE:10}
    maximum-attempts: ${OUTBOX_MAXIMUM_ATTEMPTS:20}
    retry-base-delay: ${OUTBOX_RETRY_BASE_DELAY:PT1S}
    retry-maximum-delay: ${OUTBOX_RETRY_MAXIMUM_DELAY:PT5M}
    published-retention: ${OUTBOX_PUBLISHED_RETENTION:P30D}
    cleanup-interval: ${OUTBOX_CLEANUP_INTERVAL:PT1H}
    cleanup-batch-size: ${OUTBOX_CLEANUP_BATCH_SIZE:1000}
    maximum-event-bytes: ${OUTBOX_MAXIMUM_EVENT_BYTES:262144}
    maximum-pending-age: ${OUTBOX_MAXIMUM_PENDING_AGE:PT5M}
    kafka:
      bootstrap-servers: ${OUTBOX_KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
      security-protocol: ${OUTBOX_KAFKA_SECURITY_PROTOCOL:SASL_SSL}
      allow-insecure-transport: ${OUTBOX_KAFKA_ALLOW_INSECURE_TRANSPORT:false}
      sasl-mechanism: ${OUTBOX_KAFKA_SASL_MECHANISM:SCRAM-SHA-512}
      sasl-jaas-config: ${OUTBOX_KAFKA_SASL_JAAS_CONFIG:}
      truststore-location: ${OUTBOX_KAFKA_TRUSTSTORE_LOCATION:}
      truststore-password: ${OUTBOX_KAFKA_TRUSTSTORE_PASSWORD:}
      keystore-location: ${OUTBOX_KAFKA_KEYSTORE_LOCATION:}
      keystore-password: ${OUTBOX_KAFKA_KEYSTORE_PASSWORD:}
      key-password: ${OUTBOX_KAFKA_KEY_PASSWORD:}
      delivery-timeout: ${OUTBOX_KAFKA_DELIVERY_TIMEOUT:PT5S}
      request-timeout: ${OUTBOX_KAFKA_REQUEST_TIMEOUT:PT3S}
      maximum-block: ${OUTBOX_KAFKA_MAXIMUM_BLOCK:PT1S}
  security:
    jwt:
      issuer: ${AUTH_ISSUER_URI}
      jwk-set-uri: ${AUTH_JWK_SET_URI}
      audience: ${AUTH_AUDIENCE:catalog-api}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
      group:
        readiness:
          include: readinessState,db,catalogOutbox,catalogCirculationConsumer
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  info:
    env:
      enabled: false
  metrics:
    tags:
      application: ${spring.application.name}
